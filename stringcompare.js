// const str1= prompt("enter the first string")
// const  str2= prompt("enter the second string")
// const  result = str1.toUpperCase() === str2.toUpperCase();
// if(result){
//     console.log(`${str2} and ${str1} are same`)
// }
// else{
//     console.log(`${str2} and ${str1} are not same`)
// }


const str1="mani";
const str2="shubha";
const pattern = new RegExp(str1,"gi")
const result = str2.test
if(result){
    console.log(`${str1} ${str2} are same`)
}
else{
    console.log(`${str1} ${str2} are  not same`)
}
